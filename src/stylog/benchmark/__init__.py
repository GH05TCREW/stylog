"""Benchmark subsystem (spec section 21).

Descriptive, audit-oriented benchmarking over local TOML dataset manifests:
split audits, pairwise feature-distance summaries, and transformation
stability distances, plus the verification task (decisions and PAN decision
metrics under explicit fitted verifier models -- benchmark outputs only).
The descriptive tasks carry no attribution, no probabilities, no thresholds,
no EER. Nothing is ever downloaded.
"""
